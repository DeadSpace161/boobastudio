<div class="aicard relativedisplay" data-id="{{id}}" data-type="music">
    <img src="modules/cibola8/templates/assets/song.webp" data-src="{{song.image_url}}" data-audio-url="{{song.audio_url}}" data-origin="{{song.id}}">
    <div class="card-title">{{song.title}}</div>
    <div class="loader">
        <i class="fas fa-spinner fa-2x fa-spin"></i>
    </div>   
</div>