# Cibola 8 feature inventory

Status combines the original static inspection with the current BoobaStudio runtime implementation. `Implemented` means a local UI and runtime path exists; provider configuration and Foundry permissions may still be required. `Hosted-blocked` means the remaining normal request path requires Cibola infrastructure. `Unknown` is used where minification prevents proving the final side effect.

| Feature | UI class or template | Service/controller | External endpoint | Persistence | Foundry documents affected | Current status | Recommended fork action |
|---|---|---|---|---|---|---|---|
| Direct `/c8` chat | `completion/*`, `DirectChat` | `chat` / `TextGenerationService` | Configured OpenAI-compatible `/chat/completions`, or Anthropic/Gemini adapter | `localStorage` history + ChatMessage | ChatMessage | Local provider path implemented; hosted fallback retained | Validate each provider through the existing chat UI. |
| Prose text generation/edit/format/translate | `prose_generate`, editor hooks | `TextGenerationService`, ProseMirror handlers | Configured local query/enhance; translation remains hosted/deferred | Journal/editor document source | Journal pages and rich text | Local query/enhance path; translation deferred by user scope | Preserve the existing HTML insertion contract and English-only scope. |
| Persistent GPT threads | `thread/*` | `GptThreadSheet`, local thread callbacks | Local text provider; hosted thread fallback | `JournalEntryPage.system.messages`; hosted `thread_id` preserved only when returned | JournalEntryPage custom type | Local document-backed persistence implemented, including safe local replies without hosted thread IDs | Validate multi-turn save/reload and permissions in Foundry. |
| Random prose/content | `random/*`, `prompt*` | generation application | Configured local query/enhance | local UI/history callbacks | optional Journal/Item/etc. | Local provider path implemented | Keep existing prompt builders and document previews. |
| Actor/Item/Journal generation | random/prompt/model forms | document generation controllers | Configured local query/enhance | preview then document update | Actor, Item, JournalEntry, RollTable | Local provider path implemented | Verify per-document ownership and retain copy/preview behavior. |
| Image generation | `bothgen/*`, `prompt.hbs` | `AiBothGen`, `genImage` | Configured OpenAI-compatible, Replicate, Stability, or ComfyUI adapter | local gallery/history + Foundry file upload | Actor, Item, Tile, Scene | Local provider paths implemented | Validate provider-specific models and preserve existing apply/save actions. |
| Image editing/variations/inpainting | `bothgen/canvas`, toolbar | `AiBothGen`, enhancement façade | OpenAI-compatible `/images/edits` for image/mask requests; Replicate prediction routes | local canvas state/history | image sources; optional documents | Adapter paths implemented; provider-specific UI validation remains | Keep canvas contract; add capability map and validate each configured model. |
| Background removal | `bothgen/removebg` | image enhancement controller | Configured Replicate prediction/input JSON | saved file | Actor/Item/Tile/Scene | Local adapter implemented; model configuration required | Keep the existing image-edit UI and report model/input errors clearly. |
| Upscaling | `scenegen/upscale`, image toolbar | image enhancement controller | Configured Replicate prediction/input JSON | saved file | Scene/Tile/Actor/etc. | Local adapter implemented; model/input validation remains | Preserve existing save/apply flow and document supported model inputs. |
| Scene/map generation | `scenegen/*` | `SceneRandomGen`, image façade | Configured local image provider | Foundry file + scene update | Scene | Local provider path implemented | Preserve scene background/update helper and grid settings. |
| Wall detection | `wall_detection/detect_walls` | `WallDetectionApp`, OpenCV worker | none for processing | presets/settings; scene walls | Scene walls | Fully local | Preserve; update only namespace and v14 checks. |
| Translation | `bothgen/translator`, `localizer/*` | translation queue/completion | `/translate` | queue + translated source files | Actor, Item, RollTable, Tile, JournalEntry, compendia | Deferred by user scope | Retain Foundry localization files; defer AI document translation unless scope changes. |
| Name generation | `namegen/namegen` | `CibolaNames` | Configured local query | token/actor names on save | Token/Actor | Local provider path implemented | Preserve unlinked-token grouping. |
| TTS and token voices | `song/tts`, `tokenspeaks/list` | `TTSJournalApp`, `TTSTokenSpeaksApp`, `TokenSpeaks` | Configured OpenAI TTS or ElevenLabs endpoint | `cibola8.voice` flags + Foundry audio | Actor/Token/Journal | Local adapters and local voice catalogs implemented | Keep voice flags and Foundry audio flow. |
| Music/song generation | `song/generator` | `AiMusicGen` | configurable Replicate prediction; optional model-specific input JSON | Foundry playlist/file | Playlist | Local adapter available when a Replicate music model is configured; hosted fallback remains for unconfigured mode | Configure a compatible Replicate audio model and preserve `{audio_url,image_url,lyric}` shape. |
| Gallery/browser/history | `browser/*`, partials | `BrowserForm`, pack apps | Browser-local browse/remove/index; share/public/pack actions disabled locally | browser-local metadata + local files | optional target docs | Local generated-content index implemented; community actions unavailable | Continue metadata parity without reintroducing a hosted dependency. |
| Packs/community gallery | `packs/*` | pack API/application classes | `/packs*`, `/my/packs*` | hosted pack records | tokens and generated image assets | Cibola-hosted | Disable by default or replace with Foundry-local collections; not part of first slice. |
| Vector store / semantic search | `vector/*` | vector façade + thread UI | Browser-local upload/list/delete and token-overlap retrieval | browser-local vector library | Journal threads | Local lightweight retrieval implemented; hosted semantic index unavailable | Keep the local fallback and avoid introducing a backend. |
| Token framing/revolver | `token/*`, `tokenhorde/*` | `TokenHorde`, Tokenizer integration | image source may be hosted | Actor/token data and files | Actor, Token | Partially local; optional VTTA-Tokenizer dependency | Preserve and make image source provider-neutral. |
| Prompt templates/model selectors | `modelform`, selectors, systemdefines | model/config metadata + local provider settings | local provider settings; hosted `config` fallback | form state only | all generation workflows | UI complete; local models are configurable, hosted catalog remains optional | Keep existing selectors and document provider/model settings. |
| Radial menu/macros | `radial/*`, onboarding | `RadialWidget`, macro setup | none | world/client settings and Macro flags | Macro/hotbar | Fully local | Rename macro command/flag with compatibility fallback. |
| Document context/editor integrations | ProseMirror and document hooks | `Le` and sheet hook controllers | underlying local provider routes, with hosted fallback | document updates | Actor, Item, Journal, Scene, Tile, RollTable, compendium | UI/local integration and local generation paths implemented | Preserve permission checks and preview/update flow. |
| Onboarding/account/settings | `onboarding/welcome`, `configuration`, `token/*` | local provider settings plus compatibility account controllers | local settings; hosted account fallback | client/world settings | Macro and module settings | Local setup path implemented; hosted account compatibility retained | Keep local provider configuration primary and prevent account gating. |

The requested parity list is broad but not uniformly available locally. Wall detection, UI, document integration, queue mechanics, prompt/model forms, macro/radial menu, Foundry file handling, and the validated text/image/audio workflows have local paths. Community packs, public gallery actions, and the legacy hosted translation queue remain unavailable; module UI localization remains local.
